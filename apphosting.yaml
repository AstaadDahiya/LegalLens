# App Hosting Configuration for LegalLens

# Cloud Run service configuration
runConfig:
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 4
  concurrency: 80

# Environment variables
env:
  - variable: GEMINI_API_KEY
    secret: GEMINI_API_KEY
